# Alpha soft error

Synthetic threshold model showing why a fixed particle environment can produce more memory upsets as stored-node critical charge shrinks.

Run:

```bash
python alpha_soft_error.py
```

The event distribution and charge units are invented. This is not radiation transport or SER prediction.
