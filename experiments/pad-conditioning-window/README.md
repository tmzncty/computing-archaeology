# Pad Conditioning Window

A synthetic CMP model balancing pad glazing against conditioning intensity and pad wear.

Run:

```bash
python experiments/pad-conditioning-window/pad_conditioning_window.py
```

The model is not calibrated to any pad or diamond conditioner. It demonstrates why both under-conditioning and over-conditioning can be undesirable.