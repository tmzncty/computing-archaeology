# LGA contact array

Synthetic large-contact-population model.

It demonstrates why increasing contact count, force spread, and low-force tails matter for a separable CPU socket, with a simple power-contact heating proxy.

Run:

```bash
python lga_contact_array.py
```

The force distribution and thresholds are invented and do not represent an Intel socket specification.
