# Microvia Fill

A synthetic comparison between conformal plating and bottom-biased filling in a recessed via.

Run:

```bash
python experiments/microvia-fill/microvia_fill.py
```

This is not a copper-plating simulation or bath recipe. It isolates the geometric reason bottom-up deposition can be useful in blind microvias.