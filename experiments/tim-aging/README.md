# TIM aging

Synthetic thermal-interface aging model for pump-out and dry-out style degradation.

Run:

```bash
python tim_aging.py
```

It demonstrates only that interface resistance can worsen with repeated thermal cycling through more than one mechanism. It is not a TIM service-life predictor.
