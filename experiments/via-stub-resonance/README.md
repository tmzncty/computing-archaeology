# Via-stub resonance

Simple quarter-wave proxy for an unused plated-through-hole stub.

It demonstrates why shortening a residual stub can move a destructive resonance upward in frequency.

Run:

```bash
python via_stub_resonance.py
```

This is not a full PCB via model; pads, anti-pads, return paths, losses, and mode conversion are omitted.
